language: python
ddons:
  sonarcloud:
    organization: "shantanu14011994" # the key of the org you chose at step #3
    token: ${SONAR_TOKEN}
script:
  # other script steps might be done before running the actual analysis
  - sonar-scanner
#trying
